<li>
  {{question.author}}'s Question - {{question.question}}, Topic: {{question.topic}}
  {{#if isImageShowing}}
    <p><img src={{question.image}} alt={{question.topic}} {{action 'imageHide'}}></p>
  {{else}}
    <button {{action 'imageShow'}}>Show image</button>
  {{/if}}
  <button {{action 'delete' question}}>Delete Question</button>
</li>
